package main

import (
	"fmt"
)

// arrays no se usan, se usan slices
func main() {
	//x := type {values} composite literal
	x := []int{4, 5, 7, 8, 42}
	fmt.Println(x)
	//Slices te permite agrupar VALORES del mismo TIPO juntos
}
